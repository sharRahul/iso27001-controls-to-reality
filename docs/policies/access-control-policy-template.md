# Access Control Policy Template (moved)

The maintained access control policy template now lives at [`docs/isms-policy-templates/access-control-policy.md`](../isms-policy-templates/access-control-policy.md).

The two overlapping templates were consolidated into `docs/isms-policy-templates/`, which is the canonical policy template directory. The minimum evidence table that previously lived in this file has been merged into the canonical template. This pointer file is retained only so existing links do not break and will be removed in a future release.
