# Incident Response Policy Template (moved)

The maintained incident response policy template now lives at [`docs/isms-policy-templates/incident-response-policy.md`](../isms-policy-templates/incident-response-policy.md).

The two overlapping templates were consolidated into `docs/isms-policy-templates/`, which is the canonical policy template directory. The minimum evidence table and the A.5.27 control mapping that previously lived in this file have been merged into the canonical template. This pointer file is retained only so existing links do not break and will be removed in a future release.
